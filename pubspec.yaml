name: hofswap
description: Our term project for CSC 190.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  http: 0.12.2
  path_provider: 1.6.18
  #camera: ^0.5.7+4
  #firebase_core: ^0.4.0+9   #Base library for Firebase
  firebase_ml_vision: ^0.9.6+3 #Library for Firebase Machine Learning Vision Library
  #firebase_auth: ^0.14.0+5 #Library for Authenticating with the Firebase Database
  #cloud_firestore: ^0.12.9+5 #Library for Interacting with the Firebase Database

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/logo.jpg
    - assets/hofcard.jpg
    - assets/hofcard2.jpg